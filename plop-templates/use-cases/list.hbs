import { Injectable } from '@nestjs/common';
import {
  I{{pascalCase name}}Repository,
  type List{{pascalCase name}}Input,
} from '@/modules/{{dashCase name}}/application/repositories';
import { {{pascalCase name}} } from '@/modules/{{dashCase name}}/domain';

type List{{pascalCase name}}UseCaseRequest = List{{pascalCase name}}Input;

interface List{{pascalCase name}}UseCaseResponse {
  data: {{pascalCase name}}[];
  total: number;
}

@Injectable()
export class List{{pascalCase name}}UseCase {
  constructor(private readonly repository: I{{pascalCase name}}Repository) {}

  async execute(
    input: List{{pascalCase name}}UseCaseRequest,
  ): Promise<List{{pascalCase name}}UseCaseResponse> {
    const [data, total] = await Promise.all([
      this.repository.list(input),
      this.repository.count(input),
    ]);

    return {
      data,
      total,
    };
  }
}
